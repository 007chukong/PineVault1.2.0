# 更新日志

## v1.2.4（2026-09-21）
- 新增「自动填充排除列表」：可逐应用排除，并提供「敏感页面保护」开关（默认开启，
  内置聊天/支付/银行/钱包/系统设置等高敏感应用）。排除或受保护的应用，原生填充服务
  不再生成填充建议、也不再弹出保存提示；配置通过 `autofill_settings` 通道下发到原生，
  与填充状态分文件存储。
- 新增「人脸解锁」开关：开启后设备验证仅接受面容/指纹，不再回落到系统密码；
  「设备验证解锁」弹窗改为居中留白式布局（圆形图标徽章 + 居中说明 + 等宽按钮组）。
- 新增「背景设置」：可选本地图片作为页面背景，并单独控制遮罩强度与背景模糊，
  全局生效；图片只保存在本机，不参与同步。
- 新增首次启动「免责声明」：未同意前不进入应用，同意状态本地持久化。
- 底栏填色修正：任何场景下底栏都保持半透明磨砂质感，只切换玻璃底色；
  选中胶囊改为比玻璃底色稍深一档的绿色，深色模式同步适配（mint900 玻璃 + 提亮未选中字）。

## v1.2.3（2026-09-21）

- 底部导航改为悬浮式磨砂玻璃条：上下左右均留出边距，不再贴死屏幕底边、不再全宽通底；
  圆角放大到 28，半透明浅绿底叠加背景模糊（BackdropFilter，sigma 18）与淡淡弥散阴影，
  选中项保留绿色高亮，图标 + 文字布局不变。
- 页面主体内容保持原样，仅调整底栏样式；三个页面（密码库 / 同步 / 设置）的滚动列表
  与多选操作栏按底栏实际高度预留底部余量，最后一行不再被悬浮底栏遮挡。
- 新增「屏幕下方任意区域左右滑动切换底栏项」：横向拖动切页，纵向拖动仍归列表滚动，
  点击仍精确落到对应底栏按钮。
- 设置页「关于」分组在「联系开发者」下方新增「酷安主页」条目，前置酷安应用图标，
  点击直接打开 https://www.coolapk.com/u/20634810，设备无法拉起浏览器时自动复制链接到剪贴板。
- pubspec 版本号更新为 1.2.3+14。

## v1.2.2（2026-09-20）
- 底部导航由四栏收敛为三栏：密码库 / 同步 / 设置。1.2.1 中"新建"并不是一个页面，却占了常驻 Tab，导致"新建页"里堆满了同步与设置的入口。
- 新建入口改为密码库页顶部的常驻主按钮「新建密码」，所有屏幕尺寸（含手机）都能一眼看到；空状态文案同步指向该按钮。
- 功能入口各归其位，同一功能全应用只保留一个入口：
  - "同步"页：WebDAV 云同步、立即同步、WebDAV 设置、同步历史。
  - "设置"页：解锁与安全、备份与恢复、分组管理、导入 / 导出 KDBX、显示与排序、会话、关于。
- 视觉清晰度修复（应答"太糊、看不清"）：
  - 取消描边与分割线的 alpha 稀释，卡片描边改实色 #54A17F，卡内分割线改 #8FCDAF。
  - 主色由 mint600 加深为 mint700 #2C8360；次级文字改 #55705F；底栏未选中文字 #47624F。
  - 页面底色 #DCEFE4、底栏底色 #C9E5D5，各层对比度均按 WCAG AA（≥4.5:1）实测达标。
- 「选择适用的应用」列表改为带图标展示：原生侧提取应用图标并压成 PNG（72px）经通道回传，列表项与已选标签都在应用名左侧显示图标，取不到图标时回退为通用图标。
- 设置页「数据」分组新增「清理缓存」：清空 Dart 侧应用列表缓存、原生侧图标字节流缓存与已解码图片缓存；安装 / 卸载应用后想刷新图标用它即可，不必重装应用，密码库数据不受影响。
- 设置页「关于」新增「联系开发者」条目：3153057775@qq.com，点击唤起邮件应用，设备未装邮件应用时自动复制邮箱到剪贴板。
- pubspec 版本号更新为 1.2.2+13。

## v1.2.1（2026-09-20）

- 全新的浅绿视觉体系：重写全局主题，以薄荷浅绿为品牌主色，统一圆角、间距与浅色分层卡片，取消重阴影。
- 新增「通用视觉组件」：卡片、图标徽章、区块标题、设置列表项、分隔线与统计卡，全应用复用同一套视觉语言。
- 首页交互重构：新增 iOS 风格底部四栏导航（密码库 / 新建 / 同步 / 设置），取代原有集中式右上角菜单。
- 密码库栏保留三条杠菜单：多选、展示密码、展示网站、显示 TOTP、按时间排序、按名称排序、锁定。
- 其余功能按「新建 / 同步 / 设置」重新归置，入口更易发现。
- 自动填充界面主题同步切换到浅绿体系。
- pubspec 版本号更新为 1.2.1+12。

## v1.2.0（2026-09-20）

- 密码条目新增“应用”字段：可为条目选择适用范围（网站 / 应用 / 应用及网站）。
- 新增“已安装应用”选择器，通过原生通道读取设备上可启动的应用列表（Android）。
- 自动填充匹配逻辑支持按应用包名匹配条目，应用内也可直接使用对应的账号密码。
- 编辑 / 查看 / 列表 / 操作页同步适配新增的应用字段。
- pubspec 版本号更新为 1.2.0+11。

## v1.0.9（2026-09-15）

- Android 自动填充新增保存登录信息：保存前可查看并编辑名称、用户名、密码和网站。
- 保存成功后，主 App 重新加载本地密码库，并按现有逻辑触发 WebDAV 同步。
- 修复选择填充弹窗的搜索框无法调出输入法，以及输入法弹出后的底部空白问题。
